#git init >> initialise the git repo on this directory
#git add . >> save everything
#git commit -m "description" >> this saves the changes that 
                                we added in last step
#git checkout -b new_branch >> create a new branch

